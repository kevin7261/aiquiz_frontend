# 本專案不儲存任何 API key，請在建立測驗題庫頁面輸入後使用。
# 後端專案儲存庫名稱：MyQuiz.ai_backend（見 README、src/constants/api.js）。

# 開發時 API 基底（/english_system 等）—二擇一：
# A) 不設 VUE_APP_API_BASE（預設）：直連本機 http://127.0.0.1:8000（與 Postman/curl 相同）；後端 CORS 須含前端 origin（如 http://localhost:8081）。
# B) 要經 dev server 之 webpack 代理：VUE_APP_API_BASE 設成與目前頁面 origin 相同，例如
#    VUE_APP_API_BASE=http://localhost:8081
#    （vue.config.js 會把 /english_system 轉到 DEV_API_PROXY_TARGET）

# 選填：npm run serve 時覆寫代理目標（勿結尾斜線）。預設 http://127.0.0.1:8000；/english_system 由此轉發。
# DEV_API_PROXY_TARGET=https://myquiz-ai.onrender.com

# 英文 MP3 轉逐字稿：POST /english_system/transcript/audio（後端 Deepgram；金鑰與 DEEPGRAM_MODEL 設於後端環境變數，非本前端 .env）
