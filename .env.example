# 本專案不儲存任何 API key，請在建立測驗題庫頁面輸入後使用。
# 後端專案儲存庫名稱：MyQuiz.ai_backend（見 README、src/constants/api.js）。

# 選填：強制 API 基底網址（勿結尾斜線）。未設定時，npm run serve + localhost 會用目前網址並經 vue 代理；本機生產建置預設 http://127.0.0.1:8000。
# VUE_APP_API_BASE=http://127.0.0.1:8000

# 選填：npm run serve 時覆寫 API 代理目標（勿結尾斜線）。預設已為 http://127.0.0.1:8000；僅在要連 Render 測試時再設：
# DEV_API_PROXY_TARGET=https://myquiz-ai-backend.onrender.com
